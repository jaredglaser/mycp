//
// adapted from http://www.cprogramming.com/snippets/source-code/singly-linked-list-insert-remove-add-count
//

#define  BUFFERSIZE 128
#include <unistd.h>

//node_t *head;
//node_t *tail;
//void insert(char *name, char *artist, char *date, int runtime);

int main()
{
  //int i, len, runtime;
  //struct node *n;
  //char buffer[BUFFERSIZE], artist[BUFFERSIZE], date[BUFFERSIZE], c;

  //head = NULL;

  while (1) {
    /*printf("\nList Operations\n");
    if (scanf("%d%c", &i, &c) <= 0) {          // use c to capture \n
        printf("Enter only an integer...\n");
        exit(0);
    } else {
      	if (fgets(buffer, BUFFERSIZE , stdin) != NULL) {
                  len = strlen(buffer);
                  buffer[len - 1] = '\0';   // override \n to become \0
                } else {
                    printf("not a valid name...");
                    exit(-1);
		}
	*/	
	
    
  }
  return 0;
}
